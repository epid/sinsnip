#main
  %h2 All Snippets
  - @snippets.each do |s|
    .snip
      .snip_title== <a href="/s/#{s.id}">#{s.title}</a>
      .snip_body
        = CodeRay.scan(s.summary, s.language).div
      .snip_foot
        .snip_lang= s.language
        .snip_tags
          Tags:
          - s.tags.split.each do |t|
            == <a href="/t/#{t}">#{t}</a>
